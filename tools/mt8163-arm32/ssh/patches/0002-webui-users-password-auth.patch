diff --git a/Makefile.in b/Makefile.in
--- a/Makefile.in
+++ b/Makefile.in
@@ -49,5 +49,5 @@
 _SVROBJS=svr-kex.o svr-auth.o sshpty.o \
-		svr-authpasswd.o svr-authpubkey.o svr-authpubkeyoptions.o svr-session.o svr-service.o \
+		svr-authpasswd.o libreecho-auth.o svr-authpubkey.o svr-authpubkeyoptions.o svr-session.o svr-service.o \
 		svr-chansession.o svr-runopts.o svr-agentfwd.o svr-main.o svr-x11fwd.o\
 		svr-forward.o svr-tcpfwd.o svr-streamfwd.o svr-authpam.o
 SVROBJS = $(patsubst %,$(OBJ_DIR)/%,$(_SVROBJS))
@@ -77,1 +77,1 @@
-_SCPOBJS=scp.o progressmeter.o atomicio.o scpmisc.o compat.o
+_SCPOBJS=scp.o progressmeter.o atomicio.o scpmisc.o compat.o dbutil.o
diff --git a/src/svr-authpasswd.c b/src/svr-authpasswd.c
--- a/src/svr-authpasswd.c
+++ b/src/svr-authpasswd.c
@@ -33,6 +33,9 @@
 
 #if DROPBEAR_SVR_PASSWORD_AUTH
 
+extern int libreecho_auth_password(const char *username, const char *password,
+		unsigned int password_length);
+
 /* not constant time when strings are differing lengths. 
  string content isn't leaked, and crypt hashes are predictable length. */
 static int constant_time_strcmp(const char* a, const char* b) {
@@ -51,5 +51,3 @@
 void svr_auth_password(int valid_user) {
-	
-	char * passwdcrypt = NULL; /* the crypt from /etc/passwd or /etc/shadow */
-	char * testcrypt = NULL; /* crypt generated from the user's password sent */
+	int webui_password_valid = 0;
 	char * password = NULL;
@@ -67,8 +67,7 @@
 	password = buf_getstring(ses.payload, &passwordlen);
 	if (valid_user && passwordlen <= DROPBEAR_MAX_PASSWORD_LEN) {
-		/* the first bytes of passwdcrypt are the salt */
-		passwdcrypt = ses.authstate.pw_passwd;
-		testcrypt = crypt(password, passwdcrypt);
+		webui_password_valid = libreecho_auth_password(
+			ses.authstate.pw_name, password, passwordlen);
 	}
 	m_burn(password, passwordlen);
 	m_free(password);
